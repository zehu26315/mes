<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.wages.mapper.WageRecordMapper">

    <resultMap id="BaseResultMap" type="WageRecord">
        <id property="id" column="id"/>
        <result property="employeeName" column="employee_name"/>
        <result property="employeeCode" column="employee_code"/>
        <result property="workOrderId" column="work_order_id"/>
        <result property="processCode" column="process_code"/>
        <result property="quantity" column="quantity"/>
        <result property="unitPrice" column="unit_price"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="recordDate" column="record_date"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, employee_name, employee_code, work_order_id, process_code,
        quantity, unit_price, total_amount, record_date,
        is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wages_record (employee_name, employee_code, work_order_id, process_code,
                                  quantity, unit_price, total_amount, record_date)
        VALUES (#{employeeName}, #{employeeCode}, #{workOrderId}, #{processCode},
                #{quantity}, #{unitPrice}, #{totalAmount}, #{recordDate})
    </insert>

    <update id="deleteById">
        UPDATE wages_record SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM wages_record
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM wages_record
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (employee_name LIKE CONCAT('%', #{keyword}, '%')
                 OR employee_code LIKE CONCAT('%', #{keyword}, '%')
                 OR process_code LIKE CONCAT('%', #{keyword}, '%')
                 OR work_order_id LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM wages_record
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (employee_name LIKE CONCAT('%', #{keyword}, '%')
                 OR employee_code LIKE CONCAT('%', #{keyword}, '%')
                 OR process_code LIKE CONCAT('%', #{keyword}, '%')
                 OR work_order_id LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>

</mapper>
