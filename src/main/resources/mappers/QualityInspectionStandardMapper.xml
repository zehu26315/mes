<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.quality.mapper.QualityInspectionStandardMapper">

    <resultMap id="BaseResultMap" type="QualityInspectionStandard">
        <id property="id" column="id"/>
        <result property="standardNo" column="standard_no"/>
        <result property="standardName" column="standard_name"/>
        <result property="productCode" column="product_code"/>
        <result property="customerName" column="customer_name"/>
        <result property="sampleSize" column="sample_size"/>
        <result property="aqlLevel" column="aql_level"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, standard_no, standard_name, product_code, customer_name,
        sample_size, aql_level, status,
        is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO quality_inspection_standard (standard_no, standard_name, product_code, customer_name,
                                                  sample_size, aql_level, status)
        VALUES (#{standardNo}, #{standardName}, #{productCode}, #{customerName},
                #{sampleSize}, #{aqlLevel}, #{status})
    </insert>

    <update id="update">
        UPDATE quality_inspection_standard
        <set>
            <if test="standardNo != null">standard_no = #{standardNo},</if>
            <if test="standardName != null">standard_name = #{standardName},</if>
            <if test="productCode != null">product_code = #{productCode},</if>
            <if test="customerName != null">customer_name = #{customerName},</if>
            <if test="sampleSize != null">sample_size = #{sampleSize},</if>
            <if test="aqlLevel != null">aql_level = #{aqlLevel},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE quality_inspection_standard SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM quality_inspection_standard
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM quality_inspection_standard
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (standard_name LIKE CONCAT('%', #{keyword}, '%')
                 OR standard_no LIKE CONCAT('%', #{keyword}, '%')
                 OR product_code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM quality_inspection_standard
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (standard_name LIKE CONCAT('%', #{keyword}, '%')
                 OR standard_no LIKE CONCAT('%', #{keyword}, '%')
                 OR product_code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

</mapper>
