<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.warehouse.mapper.WhWarehouseMapper">
    <resultMap id="BaseResultMap" type="WhWarehouse">
        <id property="id" column="id"/>
        <result property="whCode" column="wh_code"/>
        <result property="whName" column="wh_name"/>
        <result property="whType" column="wh_type"/>
        <result property="location" column="location"/>
        <result property="manager" column="manager"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    <sql id="BaseColumns">id, wh_code, wh_name, wh_type, location, manager, status, is_deleted, create_time, update_time</sql>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wh_warehouse (wh_code, wh_name, wh_type, location, manager, status)
        VALUES (#{whCode}, #{whName}, #{whType}, #{location}, #{manager}, #{status})
    </insert>
    <update id="update">
        UPDATE wh_warehouse
        <set>
            <if test="whName != null">wh_name = #{whName},</if>
            <if test="whType != null">wh_type = #{whType},</if>
            <if test="location != null">location = #{location},</if>
            <if test="manager != null">manager = #{manager},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>
    <update id="deleteById">UPDATE wh_warehouse SET is_deleted = 1 WHERE id = #{id}</update>
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM wh_warehouse WHERE id = #{id} AND is_deleted = 0
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM wh_warehouse WHERE is_deleted = 0 ORDER BY id ASC
    </select>
</mapper>
