<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.quality.mapper.QualityInspectionResultMapper">

    <resultMap id="BaseResultMap" type="QualityInspectionResult">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="itemId" column="item_id"/>
        <result property="itemName" column="item_name"/>
        <result property="standardValue" column="standard_value" jdbcType="DECIMAL"/>
        <result property="upperLimit" column="upper_limit" jdbcType="DECIMAL"/>
        <result property="lowerLimit" column="lower_limit" jdbcType="DECIMAL"/>
        <result property="actualValue" column="actual_value" jdbcType="DECIMAL"/>
        <result property="unit" column="unit"/>
        <result property="isPass" column="is_pass"/>
    </resultMap>

    <sql id="BaseColumns">
        id, order_id, item_id, item_name,
        standard_value, upper_limit, lower_limit, actual_value,
        unit, is_pass
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO quality_inspection_result (order_id, item_id, item_name,
                                                standard_value, upper_limit, lower_limit,
                                                actual_value, unit, is_pass)
        VALUES (#{orderId}, #{itemId}, #{itemName},
                #{standardValue,jdbcType=DECIMAL}, #{upperLimit,jdbcType=DECIMAL}, #{lowerLimit,jdbcType=DECIMAL},
                #{actualValue,jdbcType=DECIMAL}, #{unit}, #{isPass})
    </insert>

    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO quality_inspection_result (order_id, item_id, item_name,
                                                standard_value, upper_limit, lower_limit,
                                                actual_value, unit, is_pass)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.itemId}, #{item.itemName},
             #{item.standardValue,jdbcType=DECIMAL}, #{item.upperLimit,jdbcType=DECIMAL}, #{item.lowerLimit,jdbcType=DECIMAL},
             #{item.actualValue,jdbcType=DECIMAL}, #{item.unit}, #{item.isPass})
        </foreach>
    </insert>

    <update id="update">
        UPDATE quality_inspection_result
        <set>
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="itemId != null">item_id = #{itemId},</if>
            <if test="itemName != null">item_name = #{itemName},</if>
            <if test="standardValue != null">standard_value = #{standardValue,jdbcType=DECIMAL},</if>
            <if test="upperLimit != null">upper_limit = #{upperLimit,jdbcType=DECIMAL},</if>
            <if test="lowerLimit != null">lower_limit = #{lowerLimit,jdbcType=DECIMAL},</if>
            <if test="actualValue != null">actual_value = #{actualValue,jdbcType=DECIMAL},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="isPass != null">is_pass = #{isPass},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM quality_inspection_result WHERE id = #{id}
    </delete>

    <delete id="deleteByOrderId">
        DELETE FROM quality_inspection_result WHERE order_id = #{orderId}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM quality_inspection_result
        WHERE id = #{id}
    </select>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM quality_inspection_result
        WHERE order_id = #{orderId}
        ORDER BY id ASC
    </select>

</mapper>
