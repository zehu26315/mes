<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.production.mapper.ProdWorkOrderMapper">

    <resultMap id="BaseResultMap" type="ProdWorkOrder">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="productCode" column="product_code"/>
        <result property="productName" column="product_name"/>
        <result property="quantity" column="quantity" jdbcType="DECIMAL"/>
        <result property="completedQty" column="completed_qty" jdbcType="DECIMAL"/>
        <result property="defectiveQty" column="defective_qty" jdbcType="DECIMAL"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="currentProcess" column="current_process"/>
        <result property="status" column="status"/>
        <result property="workshop" column="workshop"/>
        <result property="productionLine" column="production_line"/>
        <result property="shift" column="shift"/>
        <result property="priority" column="priority"/>
        <result property="sourceOrderNo" column="source_order_no"/>
        <result property="planId" column="plan_id"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, order_no, product_code, product_name, quantity, completed_qty, defective_qty,
        start_date, end_date, current_process, status, workshop, production_line, shift,
        priority, source_order_no, plan_id, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO prod_work_order (order_no, product_code, product_name, quantity, completed_qty,
            defective_qty, start_date, end_date, current_process, status, workshop, production_line,
            shift, priority, source_order_no, plan_id)
        VALUES (#{orderNo}, #{productCode}, #{productName}, #{quantity}, #{completedQty},
            #{defectiveQty}, #{startDate}, #{endDate}, #{currentProcess}, #{status}, #{workshop},
            #{productionLine}, #{shift}, #{priority}, #{sourceOrderNo}, #{planId})
    </insert>

    <update id="update">
        UPDATE prod_work_order
        <set>
            <if test="productCode != null">product_code = #{productCode},</if>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="completedQty != null">completed_qty = #{completedQty},</if>
            <if test="defectiveQty != null">defective_qty = #{defectiveQty},</if>
            <if test="startDate != null">start_date = #{startDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="currentProcess != null">current_process = #{currentProcess},</if>
            <if test="status != null">status = #{status},</if>
            <if test="workshop != null">workshop = #{workshop},</if>
            <if test="productionLine != null">production_line = #{productionLine},</if>
            <if test="shift != null">shift = #{shift},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="sourceOrderNo != null">source_order_no = #{sourceOrderNo},</if>
            <if test="planId != null">plan_id = #{planId},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE prod_work_order SET is_deleted = 1 WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE prod_work_order SET status = #{status} WHERE id = #{id} AND is_deleted = 0
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM prod_work_order
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByOrderNo" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM prod_work_order
        WHERE order_no = #{orderNo} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM prod_work_order
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (order_no LIKE CONCAT('%', #{keyword}, '%')
                 OR product_code LIKE CONCAT('%', #{keyword}, '%')
                 OR product_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM prod_work_order
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (order_no LIKE CONCAT('%', #{keyword}, '%')
                 OR product_code LIKE CONCAT('%', #{keyword}, '%')
                 OR product_name LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

</mapper>
