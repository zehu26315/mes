<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.process.mapper.ProcessStepMapper">

    <resultMap id="BaseResultMap" type="ProcessStep">
        <id property="id" column="id"/>
        <result property="routeId" column="route_id"/>
        <result property="stepCode" column="step_code"/>
        <result property="stepName" column="step_name"/>
        <result property="stepType" column="step_type"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="workCenter" column="work_center"/>
        <result property="standardTime" column="standard_time" jdbcType="DECIMAL"/>
        <result property="yieldRate" column="yield_rate" jdbcType="DECIMAL"/>
        <result property="sopFile" column="sop_file"/>
        <result property="prevStepId" column="prev_step_id"/>
        <result property="nextStepId" column="next_step_id"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, route_id, step_code, step_name, step_type, sort_order,
        work_center, standard_time, yield_rate, sop_file, prev_step_id, next_step_id,
        is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO process_step (route_id, step_code, step_name, step_type, sort_order,
                                  work_center, standard_time, yield_rate, sop_file,
                                  prev_step_id, next_step_id)
        VALUES (#{routeId}, #{stepCode}, #{stepName}, #{stepType}, #{sortOrder},
                #{workCenter}, #{standardTime}, #{yieldRate}, #{sopFile},
                #{prevStepId}, #{nextStepId})
    </insert>

    <update id="update">
        UPDATE process_step
        <set>
            <if test="stepName != null">step_name = #{stepName},</if>
            <if test="stepType != null">step_type = #{stepType},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="workCenter != null">work_center = #{workCenter},</if>
            <if test="standardTime != null">standard_time = #{standardTime},</if>
            <if test="yieldRate != null">yield_rate = #{yieldRate},</if>
            <if test="sopFile != null">sop_file = #{sopFile},</if>
            <if test="prevStepId != null">prev_step_id = #{prevStepId},</if>
            <if test="nextStepId != null">next_step_id = #{nextStepId},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE process_step SET is_deleted = 1 WHERE id = #{id}
    </update>

    <update id="updateSortOrder">
        UPDATE process_step SET sort_order = #{sortOrder} WHERE id = #{id} AND is_deleted = 0
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM process_step
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectByRouteId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM process_step
        WHERE route_id = #{routeId} AND is_deleted = 0
        ORDER BY sort_order ASC
    </select>

</mapper>
