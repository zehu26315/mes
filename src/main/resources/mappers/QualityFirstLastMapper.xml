<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.quality.mapper.QualityFirstLastMapper">

    <resultMap id="BaseResultMap" type="QualityFirstLast">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="workOrderId" column="work_order_id"/>
        <result property="inspectionType" column="inspection_type"/>
        <result property="productCode" column="product_code"/>
        <result property="processCode" column="process_code"/>
        <result property="equipmentId" column="equipment_id"/>
        <result property="result" column="result"/>
        <result property="inspector" column="inspector"/>
        <result property="inspectionTime" column="inspection_time"/>
        <result property="measurementData" column="measurement_data"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, order_no, work_order_id, inspection_type, product_code, process_code,
        equipment_id, result, inspector, inspection_time, measurement_data,
        is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO quality_first_last (order_no, work_order_id, inspection_type,
                                         product_code, process_code, equipment_id,
                                         result, inspector, inspection_time, measurement_data)
        VALUES (#{orderNo}, #{workOrderId}, #{inspectionType},
                #{productCode}, #{processCode}, #{equipmentId},
                #{result}, #{inspector}, #{inspectionTime}, #{measurementData})
    </insert>

    <update id="update">
        UPDATE quality_first_last
        <set>
            <if test="workOrderId != null">work_order_id = #{workOrderId},</if>
            <if test="inspectionType != null">inspection_type = #{inspectionType},</if>
            <if test="productCode != null">product_code = #{productCode},</if>
            <if test="processCode != null">process_code = #{processCode},</if>
            <if test="equipmentId != null">equipment_id = #{equipmentId},</if>
            <if test="result != null">result = #{result},</if>
            <if test="inspector != null">inspector = #{inspector},</if>
            <if test="inspectionTime != null">inspection_time = #{inspectionTime},</if>
            <if test="measurementData != null">measurement_data = #{measurementData},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE quality_first_last SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM quality_first_last
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM quality_first_last
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (order_no LIKE CONCAT('%', #{keyword}, '%')
                 OR product_code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="workOrderId != null">
            AND work_order_id = #{workOrderId}
        </if>
        <if test="result != null and result != ''">
            AND result = #{result}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM quality_first_last
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (order_no LIKE CONCAT('%', #{keyword}, '%')
                 OR product_code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="workOrderId != null">
            AND work_order_id = #{workOrderId}
        </if>
        <if test="result != null and result != ''">
            AND result = #{result}
        </if>
    </select>

</mapper>
