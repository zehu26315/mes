<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.equipment.mapper.EquipMaintenancePlanMapper">

    <resultMap id="BaseResultMap" type="EquipMaintenancePlan">
        <id property="id" column="id"/>
        <result property="planNo" column="plan_no"/>
        <result property="equipId" column="equip_id"/>
        <result property="maintenanceType" column="maintenance_type"/>
        <result property="planDate" column="plan_date"/>
        <result property="content" column="content"/>
        <result property="responsiblePerson" column="responsible_person"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, plan_no, equip_id, maintenance_type, plan_date,
        content, responsible_person, status, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO equip_maintenance_plan (plan_no, equip_id, maintenance_type, plan_date, content, responsible_person, status)
        VALUES (#{planNo}, #{equipId}, #{maintenanceType}, #{planDate}, #{content}, #{responsiblePerson}, #{status})
    </insert>

    <update id="update">
        UPDATE equip_maintenance_plan
        <set>
            <if test="planNo != null">plan_no = #{planNo},</if>
            <if test="equipId != null">equip_id = #{equipId},</if>
            <if test="maintenanceType != null">maintenance_type = #{maintenanceType},</if>
            <if test="planDate != null">plan_date = #{planDate},</if>
            <if test="content != null">content = #{content},</if>
            <if test="responsiblePerson != null">responsible_person = #{responsiblePerson},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE equip_maintenance_plan SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM equip_maintenance_plan
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM equip_maintenance_plan
        WHERE is_deleted = 0
        <if test="equipId != null">
            AND equip_id = #{equipId}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="startDate != null">
            AND plan_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            AND plan_date &lt;= #{endDate}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM equip_maintenance_plan
        WHERE is_deleted = 0
        <if test="equipId != null">
            AND equip_id = #{equipId}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="startDate != null">
            AND plan_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            AND plan_date &lt;= #{endDate}
        </if>
    </select>

</mapper>
