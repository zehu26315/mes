<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.equipment.mapper.EquipInspectionPlanMapper">

    <resultMap id="BaseResultMap" type="EquipInspectionPlan">
        <id property="id" column="id"/>
        <result property="planNo" column="plan_no"/>
        <result property="equipId" column="equip_id"/>
        <result property="inspectionType" column="inspection_type"/>
        <result property="planDate" column="plan_date"/>
        <result property="checkPoints" column="check_points"/>
        <result property="responsiblePerson" column="responsible_person"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, plan_no, equip_id, inspection_type, plan_date,
        check_points, responsible_person, status, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO equip_inspection_plan (plan_no, equip_id, inspection_type, plan_date, check_points, responsible_person, status)
        VALUES (#{planNo}, #{equipId}, #{inspectionType}, #{planDate}, #{checkPoints}, #{responsiblePerson}, #{status})
    </insert>

    <update id="update">
        UPDATE equip_inspection_plan
        <set>
            <if test="planNo != null">plan_no = #{planNo},</if>
            <if test="equipId != null">equip_id = #{equipId},</if>
            <if test="inspectionType != null">inspection_type = #{inspectionType},</if>
            <if test="planDate != null">plan_date = #{planDate},</if>
            <if test="checkPoints != null">check_points = #{checkPoints},</if>
            <if test="responsiblePerson != null">responsible_person = #{responsiblePerson},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE equip_inspection_plan SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM equip_inspection_plan
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM equip_inspection_plan
        WHERE is_deleted = 0
        ORDER BY id DESC
    </select>

</mapper>
