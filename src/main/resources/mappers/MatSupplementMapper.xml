<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.material.mapper.MatSupplementMapper">

    <resultMap id="BaseResultMap" type="com.example.mes.modules.material.entity.MatSupplement">
        <id property="id" column="id"/>
        <result property="suppNo" column="supp_no"/>
        <result property="workOrderId" column="work_order_id"/>
        <result property="materialId" column="material_id"/>
        <result property="materialCode" column="material_code"/>
        <result property="materialName" column="material_name"/>
        <result property="suppQty" column="supp_qty"/>
        <result property="unit" column="unit"/>
        <result property="suppReason" column="supp_reason"/>
        <result property="status" column="status"/>
        <result property="applicant" column="applicant"/>
        <result property="approver" column="approver"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, supp_no, work_order_id, material_id, material_code, material_name,
        supp_qty, unit, supp_reason, status, applicant, approver,
        is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mat_supplement (supp_no, work_order_id, material_id, material_code, material_name,
                                    supp_qty, unit, supp_reason, status, applicant, approver)
        VALUES (#{suppNo}, #{workOrderId}, #{materialId}, #{materialCode}, #{materialName},
                #{suppQty,jdbcType=DECIMAL}, #{unit}, #{suppReason}, #{status}, #{applicant}, #{approver})
    </insert>

    <update id="update">
        UPDATE mat_supplement
        <set>
            <if test="workOrderId != null">work_order_id = #{workOrderId},</if>
            <if test="materialId != null">material_id = #{materialId},</if>
            <if test="materialCode != null">material_code = #{materialCode},</if>
            <if test="materialName != null">material_name = #{materialName},</if>
            <if test="suppQty != null">supp_qty = #{suppQty,jdbcType=DECIMAL},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="suppReason != null">supp_reason = #{suppReason},</if>
            <if test="status != null">status = #{status},</if>
            <if test="applicant != null">applicant = #{applicant},</if>
            <if test="approver != null">approver = #{approver},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE mat_supplement SET is_deleted = 1 WHERE id = #{id}
    </update>

    <update id="approve">
        UPDATE mat_supplement SET status = 'APPROVED' WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM mat_supplement
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM mat_supplement
        WHERE is_deleted = 0
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="workOrderId != null">
            AND work_order_id = #{workOrderId}
        </if>
        <if test="materialCode != null and materialCode != ''">
            AND material_code = #{materialCode}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM mat_supplement
        WHERE is_deleted = 0
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="workOrderId != null">
            AND work_order_id = #{workOrderId}
        </if>
        <if test="materialCode != null and materialCode != ''">
            AND material_code = #{materialCode}
        </if>
    </select>

</mapper>
