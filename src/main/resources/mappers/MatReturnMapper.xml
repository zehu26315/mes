<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.material.mapper.MatReturnMapper">

    <resultMap id="BaseResultMap" type="com.example.mes.modules.material.entity.MatReturn">
        <id property="id" column="id"/>
        <result property="returnNo" column="return_no"/>
        <result property="workOrderId" column="work_order_id"/>
        <result property="reqId" column="req_id"/>
        <result property="materialId" column="material_id"/>
        <result property="materialCode" column="material_code"/>
        <result property="materialName" column="material_name"/>
        <result property="returnQty" column="return_qty"/>
        <result property="unit" column="unit"/>
        <result property="returnReason" column="return_reason"/>
        <result property="warehouse" column="warehouse"/>
        <result property="status" column="status"/>
        <result property="applicant" column="applicant"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, return_no, work_order_id, req_id, material_id, material_code, material_name,
        return_qty, unit, return_reason, warehouse, status, applicant,
        is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mat_return (return_no, work_order_id, req_id, material_id, material_code, material_name,
                                return_qty, unit, return_reason, warehouse, status, applicant)
        VALUES (#{returnNo}, #{workOrderId}, #{reqId}, #{materialId}, #{materialCode}, #{materialName},
                #{returnQty,jdbcType=DECIMAL}, #{unit}, #{returnReason}, #{warehouse}, #{status}, #{applicant})
    </insert>

    <update id="update">
        UPDATE mat_return
        <set>
            <if test="workOrderId != null">work_order_id = #{workOrderId},</if>
            <if test="reqId != null">req_id = #{reqId},</if>
            <if test="materialId != null">material_id = #{materialId},</if>
            <if test="materialCode != null">material_code = #{materialCode},</if>
            <if test="materialName != null">material_name = #{materialName},</if>
            <if test="returnQty != null">return_qty = #{returnQty,jdbcType=DECIMAL},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="returnReason != null">return_reason = #{returnReason},</if>
            <if test="warehouse != null">warehouse = #{warehouse},</if>
            <if test="status != null">status = #{status},</if>
            <if test="applicant != null">applicant = #{applicant},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE mat_return SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM mat_return
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM mat_return
        WHERE is_deleted = 0
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="workOrderId != null">
            AND work_order_id = #{workOrderId}
        </if>
        <if test="materialCode != null and materialCode != ''">
            AND material_code = #{materialCode}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM mat_return
        WHERE is_deleted = 0
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="workOrderId != null">
            AND work_order_id = #{workOrderId}
        </if>
        <if test="materialCode != null and materialCode != ''">
            AND material_code = #{materialCode}
        </if>
    </select>

</mapper>
