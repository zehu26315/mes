<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.material.mapper.MatInventoryLogMapper">

    <resultMap id="BaseResultMap" type="com.example.mes.modules.material.entity.MatInventoryLog">
        <id property="id" column="id"/>
        <result property="materialId" column="material_id"/>
        <result property="materialCode" column="material_code"/>
        <result property="changeType" column="change_type"/>
        <result property="changeQty" column="change_qty"/>
        <result property="beforeQty" column="before_qty"/>
        <result property="afterQty" column="after_qty"/>
        <result property="referenceNo" column="reference_no"/>
        <result property="operator" column="operator"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, material_id, material_code, change_type, change_qty,
        before_qty, after_qty, reference_no, operator, create_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mat_inventory_log (material_id, material_code, change_type, change_qty,
                                       before_qty, after_qty, reference_no, operator)
        VALUES (#{materialId}, #{materialCode}, #{changeType}, #{changeQty,jdbcType=DECIMAL},
                #{beforeQty,jdbcType=DECIMAL}, #{afterQty,jdbcType=DECIMAL}, #{referenceNo}, #{operator})
    </insert>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM mat_inventory_log
        WHERE 1 = 1
        <if test="materialId != null">
            AND material_id = #{materialId}
        </if>
        <if test="materialCode != null and materialCode != ''">
            AND material_code = #{materialCode}
        </if>
        <if test="changeType != null and changeType != ''">
            AND change_type = #{changeType}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM mat_inventory_log
        WHERE 1 = 1
        <if test="materialId != null">
            AND material_id = #{materialId}
        </if>
        <if test="materialCode != null and materialCode != ''">
            AND material_code = #{materialCode}
        </if>
        <if test="changeType != null and changeType != ''">
            AND change_type = #{changeType}
        </if>
    </select>

</mapper>
