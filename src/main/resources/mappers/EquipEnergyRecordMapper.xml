<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.equipment.mapper.EquipEnergyRecordMapper">

    <resultMap id="BaseResultMap" type="EquipEnergyRecord">
        <id property="id" column="id"/>
        <result property="equipId" column="equip_id"/>
        <result property="recordDate" column="record_date"/>
        <result property="recordHour" column="record_hour"/>
        <result property="electricity" column="electricity" jdbcType="DECIMAL"/>
        <result property="water" column="water" jdbcType="DECIMAL"/>
        <result property="gas" column="gas" jdbcType="DECIMAL"/>
        <result property="compressedAir" column="compressed_air" jdbcType="DECIMAL"/>
        <result property="steam" column="steam" jdbcType="DECIMAL"/>
        <result property="outputQty" column="output_qty" jdbcType="DECIMAL"/>
        <result property="unitEnergy" column="unit_energy" jdbcType="DECIMAL"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, equip_id, record_date, record_hour, electricity,
        water, gas, compressed_air, steam, output_qty, unit_energy, create_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO equip_energy_record (equip_id, record_date, record_hour, electricity,
            water, gas, compressed_air, steam, output_qty, unit_energy)
        VALUES (#{equipId}, #{recordDate}, #{recordHour}, #{electricity},
            #{water}, #{gas}, #{compressedAir}, #{steam}, #{outputQty}, #{unitEnergy})
    </insert>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM equip_energy_record
        WHERE 1 = 1
        <if test="equipId != null">
            AND equip_id = #{equipId}
        </if>
        <if test="startDate != null">
            AND record_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            AND record_date &lt;= #{endDate}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM equip_energy_record
        WHERE 1 = 1
        <if test="equipId != null">
            AND equip_id = #{equipId}
        </if>
        <if test="startDate != null">
            AND record_date &gt;= #{startDate}
        </if>
        <if test="endDate != null">
            AND record_date &lt;= #{endDate}
        </if>
    </select>

</mapper>
