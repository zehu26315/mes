<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.process.mapper.ProcessRouteMapper">

    <resultMap id="BaseResultMap" type="ProcessRoute">
        <id property="id" column="id"/>
        <result property="routeCode" column="route_code"/>
        <result property="routeName" column="route_name"/>
        <result property="productCode" column="product_code"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, route_code, route_name, product_code, description,
        status, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO process_route (route_code, route_name, product_code, description, status)
        VALUES (#{routeCode}, #{routeName}, #{productCode}, #{description}, #{status})
    </insert>

    <update id="update">
        UPDATE process_route
        <set>
            <if test="routeName != null">route_name = #{routeName},</if>
            <if test="productCode != null">product_code = #{productCode},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE process_route SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM process_route
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM process_route
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (route_code LIKE CONCAT('%', #{keyword}, '%')
                 OR route_name LIKE CONCAT('%', #{keyword}, '%')
                 OR product_code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM process_route
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (route_code LIKE CONCAT('%', #{keyword}, '%')
                 OR route_name LIKE CONCAT('%', #{keyword}, '%')
                 OR product_code LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

</mapper>
