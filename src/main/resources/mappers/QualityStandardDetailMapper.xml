<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.quality.mapper.QualityStandardDetailMapper">

    <resultMap id="BaseResultMap" type="QualityStandardDetail">
        <id property="id" column="id"/>
        <result property="standardId" column="standard_id"/>
        <result property="itemId" column="item_id"/>
        <result property="standardValue" column="standard_value" jdbcType="DECIMAL"/>
        <result property="upperLimit" column="upper_limit" jdbcType="DECIMAL"/>
        <result property="lowerLimit" column="lower_limit" jdbcType="DECIMAL"/>
    </resultMap>

    <sql id="BaseColumns">
        id, standard_id, item_id, standard_value, upper_limit, lower_limit
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO quality_standard_detail (standard_id, item_id, standard_value, upper_limit, lower_limit)
        VALUES (#{standardId}, #{itemId},
                #{standardValue,jdbcType=DECIMAL}, #{upperLimit,jdbcType=DECIMAL}, #{lowerLimit,jdbcType=DECIMAL})
    </insert>

    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO quality_standard_detail (standard_id, item_id, standard_value, upper_limit, lower_limit)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.standardId}, #{item.itemId},
             #{item.standardValue,jdbcType=DECIMAL}, #{item.upperLimit,jdbcType=DECIMAL}, #{item.lowerLimit,jdbcType=DECIMAL})
        </foreach>
    </insert>

    <update id="update">
        UPDATE quality_standard_detail
        <set>
            <if test="standardId != null">standard_id = #{standardId},</if>
            <if test="itemId != null">item_id = #{itemId},</if>
            <if test="standardValue != null">standard_value = #{standardValue,jdbcType=DECIMAL},</if>
            <if test="upperLimit != null">upper_limit = #{upperLimit,jdbcType=DECIMAL},</if>
            <if test="lowerLimit != null">lower_limit = #{lowerLimit,jdbcType=DECIMAL},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM quality_standard_detail WHERE id = #{id}
    </delete>

    <delete id="deleteByStandardId">
        DELETE FROM quality_standard_detail WHERE standard_id = #{standardId}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM quality_standard_detail
        WHERE id = #{id}
    </select>

    <select id="selectByStandardId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM quality_standard_detail
        WHERE standard_id = #{standardId}
        ORDER BY id ASC
    </select>

</mapper>
