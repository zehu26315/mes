<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.equipment.mapper.EquipRepairMapper">

    <resultMap id="BaseResultMap" type="EquipRepair">
        <id property="id" column="id"/>
        <result property="repairNo" column="repair_no"/>
        <result property="equipId" column="equip_id"/>
        <result property="faultId" column="fault_id"/>
        <result property="faultDesc" column="fault_desc"/>
        <result property="repairType" column="repair_type"/>
        <result property="reporter" column="reporter"/>
        <result property="repairPerson" column="repair_person"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="downtime" column="downtime"/>
        <result property="repairContent" column="repair_content"/>
        <result property="partsUsed" column="parts_used"/>
        <result property="cost" column="cost" jdbcType="DECIMAL"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, repair_no, equip_id, fault_id, fault_desc,
        repair_type, reporter, repair_person, start_time, end_time,
        downtime, repair_content, parts_used, cost, status,
        is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO equip_repair (repair_no, equip_id, fault_id, fault_desc,
            repair_type, reporter, repair_person, start_time, end_time,
            downtime, repair_content, parts_used, cost, status)
        VALUES (#{repairNo}, #{equipId}, #{faultId}, #{faultDesc},
            #{repairType}, #{reporter}, #{repairPerson}, #{startTime}, #{endTime},
            #{downtime}, #{repairContent}, #{partsUsed}, #{cost}, #{status})
    </insert>

    <update id="update">
        UPDATE equip_repair
        <set>
            <if test="repairNo != null">repair_no = #{repairNo},</if>
            <if test="equipId != null">equip_id = #{equipId},</if>
            <if test="faultId != null">fault_id = #{faultId},</if>
            <if test="faultDesc != null">fault_desc = #{faultDesc},</if>
            <if test="repairType != null">repair_type = #{repairType},</if>
            <if test="reporter != null">reporter = #{reporter},</if>
            <if test="repairPerson != null">repair_person = #{repairPerson},</if>
            <if test="startTime != null">start_time = #{startTime},</if>
            <if test="endTime != null">end_time = #{endTime},</if>
            <if test="downtime != null">downtime = #{downtime},</if>
            <if test="repairContent != null">repair_content = #{repairContent},</if>
            <if test="partsUsed != null">parts_used = #{partsUsed},</if>
            <if test="cost != null">cost = #{cost},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE equip_repair SET is_deleted = 1 WHERE id = #{id}
    </update>

    <update id="complete">
        UPDATE equip_repair
        SET end_time = #{endTime}, downtime = #{downtime}, status = 'COMPLETED'
        WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM equip_repair
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM equip_repair
        WHERE is_deleted = 0
        <if test="equipId != null">
            AND equip_id = #{equipId}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM equip_repair
        WHERE is_deleted = 0
        <if test="equipId != null">
            AND equip_id = #{equipId}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
    </select>

</mapper>
