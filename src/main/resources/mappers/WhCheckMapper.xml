<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.warehouse.mapper.WhCheckMapper">
    <resultMap id="BaseResultMap" type="WhCheck">
        <id property="id" column="id"/>
        <result property="checkNo" column="check_no"/>
        <result property="warehouseId" column="warehouse_id"/>
        <result property="checkType" column="check_type"/>
        <result property="checkDate" column="check_date"/>
        <result property="materialCode" column="material_code"/>
        <result property="bookQty" column="book_qty" jdbcType="DECIMAL"/>
        <result property="actualQty" column="actual_qty" jdbcType="DECIMAL"/>
        <result property="diffQty" column="diff_qty" jdbcType="DECIMAL"/>
        <result property="diffReason" column="diff_reason"/>
        <result property="status" column="status"/>
        <result property="checker" column="checker"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    <sql id="BaseColumns">id, check_no, warehouse_id, check_type, check_date, material_code, book_qty, actual_qty, diff_qty, diff_reason, status, checker, is_deleted, create_time, update_time</sql>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wh_check (check_no, warehouse_id, check_type, check_date, material_code, book_qty, actual_qty, diff_qty, diff_reason, status, checker)
        VALUES (#{checkNo}, #{warehouseId}, #{checkType}, #{checkDate}, #{materialCode}, #{bookQty}, #{actualQty}, #{diffQty}, #{diffReason}, #{status}, #{checker})
    </insert>
    <update id="update">
        UPDATE wh_check SET status = #{status}, checker = #{checker} WHERE id = #{id} AND is_deleted = 0
    </update>
    <update id="deleteById">UPDATE wh_check SET is_deleted = 1 WHERE id = #{id}</update>
    <update id="confirm">UPDATE wh_check SET status = 'CONFIRMED' WHERE id = #{id} AND is_deleted = 0</update>
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM wh_check WHERE id = #{id} AND is_deleted = 0
    </select>
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM wh_check WHERE is_deleted = 0
        <if test="warehouseId != null">AND warehouse_id = #{warehouseId}</if>
        <if test="status != null and status != ''">AND status = #{status}</if>
        ORDER BY id DESC LIMIT #{offset}, #{limit}
    </select>
    <select id="countList" resultType="long">
        SELECT COUNT(*) FROM wh_check WHERE is_deleted = 0
        <if test="warehouseId != null">AND warehouse_id = #{warehouseId}</if>
        <if test="status != null and status != ''">AND status = #{status}</if>
    </select>
</mapper>
