<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.system.mapper.SysDepartmentMapper">

    <resultMap id="BaseResultMap" type="SysDepartment">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="deptName" column="dept_name"/>
        <result property="deptCode" column="dept_code"/>
        <result property="leader" column="leader"/>
        <result property="phone" column="phone"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, parent_id, dept_name, dept_code, leader, phone,
        sort_order, status, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_department (parent_id, dept_name, dept_code, leader, phone,
                                    sort_order, status)
        VALUES (#{parentId}, #{deptName}, #{deptCode}, #{leader}, #{phone},
                #{sortOrder}, #{status})
    </insert>

    <update id="update">
        UPDATE sys_department
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="deptName != null">dept_name = #{deptName},</if>
            <if test="deptCode != null">dept_code = #{deptCode},</if>
            <if test="leader != null">leader = #{leader},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE sys_department SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM sys_department
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM sys_department
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (dept_name LIKE CONCAT('%', #{keyword}, '%')
                 OR dept_code LIKE CONCAT('%', #{keyword}, '%')
                 OR leader LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort_order ASC, id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM sys_department
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (dept_name LIKE CONCAT('%', #{keyword}, '%')
                 OR dept_code LIKE CONCAT('%', #{keyword}, '%')
                 OR leader LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

</mapper>
