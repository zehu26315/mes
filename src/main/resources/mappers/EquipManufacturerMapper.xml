<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.equipment.mapper.EquipManufacturerMapper">

    <resultMap id="BaseResultMap" type="EquipManufacturer">
        <id property="id" column="id"/>
        <result property="manufacturerName" column="manufacturer_name"/>
        <result property="manufacturerCode" column="manufacturer_code"/>
        <result property="contactPerson" column="contact_person"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="address" column="address"/>
        <result property="status" column="status"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, manufacturer_name, manufacturer_code, contact_person, contact_phone,
        address, status, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO equip_manufacturer (manufacturer_name, manufacturer_code, contact_person, contact_phone, address, status)
        VALUES (#{manufacturerName}, #{manufacturerCode}, #{contactPerson}, #{contactPhone}, #{address}, #{status})
    </insert>

    <update id="update">
        UPDATE equip_manufacturer
        <set>
            <if test="manufacturerName != null">manufacturer_name = #{manufacturerName},</if>
            <if test="manufacturerCode != null">manufacturer_code = #{manufacturerCode},</if>
            <if test="contactPerson != null">contact_person = #{contactPerson},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="address != null">address = #{address},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE equip_manufacturer SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM equip_manufacturer
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM equip_manufacturer
        WHERE is_deleted = 0
        ORDER BY id DESC
    </select>

</mapper>
