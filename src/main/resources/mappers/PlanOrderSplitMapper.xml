<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.plan.mapper.PlanOrderSplitMapper">

    <resultMap id="BaseResultMap" type="PlanOrderSplit">
        <id property="id" column="id"/>
        <result property="sourceOrderNo" column="source_order_no"/>
        <result property="childOrderNo" column="child_order_no"/>
        <result property="sourceQty" column="source_qty"/>
        <result property="splitQty" column="split_qty"/>
        <result property="splitDate" column="split_date"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, source_order_no, child_order_no, source_qty, split_qty, split_date,
        is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO plan_order_split (source_order_no, child_order_no, source_qty,
                                       split_qty, split_date)
        VALUES (#{sourceOrderNo}, #{childOrderNo}, #{sourceQty},
                #{splitQty}, #{splitDate})
    </insert>

    <update id="update">
        UPDATE plan_order_split
        <set>
            <if test="childOrderNo != null">child_order_no = #{childOrderNo},</if>
            <if test="sourceQty != null">source_qty = #{sourceQty},</if>
            <if test="splitQty != null">split_qty = #{splitQty},</if>
            <if test="splitDate != null">split_date = #{splitDate},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE plan_order_split SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM plan_order_split
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM plan_order_split
        WHERE is_deleted = 0
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM plan_order_split
        WHERE is_deleted = 0
    </select>

</mapper>
