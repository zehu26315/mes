<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.production.mapper.ProdDispatchOrderMapper">

    <resultMap id="BaseResultMap" type="ProdDispatchOrder">
        <id property="id" column="id"/>
        <result property="dispatchNo" column="dispatch_no"/>
        <result property="workOrderId" column="work_order_id"/>
        <result property="productCode" column="product_code"/>
        <result property="quantity" column="quantity" jdbcType="DECIMAL"/>
        <result property="processCode" column="process_code"/>
        <result property="processName" column="process_name"/>
        <result property="workCenter" column="work_center"/>
        <result property="equipmentId" column="equipment_id"/>
        <result property="assignedTo" column="assigned_to"/>
        <result property="status" column="status"/>
        <result property="planStartTime" column="plan_start_time"/>
        <result property="planEndTime" column="plan_end_time"/>
        <result property="actualStartTime" column="actual_start_time"/>
        <result property="actualEndTime" column="actual_end_time"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, dispatch_no, work_order_id, product_code, quantity, process_code, process_name,
        work_center, equipment_id, assigned_to, status, plan_start_time, plan_end_time,
        actual_start_time, actual_end_time, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO prod_dispatch_order (dispatch_no, work_order_id, product_code, quantity,
            process_code, process_name, work_center, equipment_id, assigned_to, status,
            plan_start_time, plan_end_time)
        VALUES (#{dispatchNo}, #{workOrderId}, #{productCode}, #{quantity},
            #{processCode}, #{processName}, #{workCenter}, #{equipmentId}, #{assignedTo},
            #{status}, #{planStartTime}, #{planEndTime})
    </insert>

    <update id="update">
        UPDATE prod_dispatch_order
        <set>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="processCode != null">process_code = #{processCode},</if>
            <if test="processName != null">process_name = #{processName},</if>
            <if test="workCenter != null">work_center = #{workCenter},</if>
            <if test="equipmentId != null">equipment_id = #{equipmentId},</if>
            <if test="assignedTo != null">assigned_to = #{assignedTo},</if>
            <if test="status != null">status = #{status},</if>
            <if test="planStartTime != null">plan_start_time = #{planStartTime},</if>
            <if test="planEndTime != null">plan_end_time = #{planEndTime},</if>
            <if test="actualStartTime != null">actual_start_time = #{actualStartTime},</if>
            <if test="actualEndTime != null">actual_end_time = #{actualEndTime},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE prod_dispatch_order SET is_deleted = 1 WHERE id = #{id}
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM prod_dispatch_order
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM prod_dispatch_order
        WHERE is_deleted = 0
        <if test="workOrderId != null">
            AND work_order_id = #{workOrderId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM prod_dispatch_order
        WHERE is_deleted = 0
        <if test="workOrderId != null">
            AND work_order_id = #{workOrderId}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

</mapper>
