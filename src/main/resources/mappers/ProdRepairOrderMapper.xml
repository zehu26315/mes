<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mes.modules.production.mapper.ProdRepairOrderMapper">

    <resultMap id="BaseResultMap" type="ProdRepairOrder">
        <id property="id" column="id"/>
        <result property="repairNo" column="repair_no"/>
        <result property="workOrderId" column="work_order_id"/>
        <result property="productCode" column="product_code"/>
        <result property="quantity" column="quantity" jdbcType="DECIMAL"/>
        <result property="defectDesc" column="defect_desc"/>
        <result property="repairProcess" column="repair_process"/>
        <result property="status" column="status"/>
        <result property="result" column="result"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="BaseColumns">
        id, repair_no, work_order_id, product_code, quantity, defect_desc, repair_process,
        status, result, is_deleted, create_time, update_time
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO prod_repair_order (repair_no, work_order_id, product_code, quantity,
            defect_desc, repair_process, status, result)
        VALUES (#{repairNo}, #{workOrderId}, #{productCode}, #{quantity},
            #{defectDesc}, #{repairProcess}, #{status}, #{result})
    </insert>

    <update id="update">
        UPDATE prod_repair_order
        <set>
            <if test="defectDesc != null">defect_desc = #{defectDesc},</if>
            <if test="repairProcess != null">repair_process = #{repairProcess},</if>
            <if test="status != null">status = #{status},</if>
            <if test="result != null">result = #{result},</if>
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="deleteById">
        UPDATE prod_repair_order SET is_deleted = 1 WHERE id = #{id}
    </update>

    <update id="complete">
        UPDATE prod_repair_order SET result = #{result}, status = 'COMPLETED'
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM prod_repair_order
        WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM prod_repair_order
        WHERE is_deleted = 0
        ORDER BY id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM prod_repair_order
        WHERE is_deleted = 0
    </select>

</mapper>
